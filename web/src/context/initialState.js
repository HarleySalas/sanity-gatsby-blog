import selectedProjectInitialState from "./selectedProject/selectedProject.initialState";
import sharedStateInitialState from "./sharedState/sharedState.initialState";
const initialState = {
  selectedProject: selectedProjectInitialState,
  sharedState: sharedStateInitialState,
};

export default initialState;
